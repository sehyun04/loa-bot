# gempago/vision

젬 가공 화면에서 "다음 항목" 4개를 읽는다. **범용 OCR 을 쓰지 않는다.**

## 왜 OCR 을 버렸나

`web/gempago-poc` 가 Tesseract 로 만든 버전이다. 실제 게임 캡처로 7라운드를 돌렸는데
매 라운드마다 **전에 없던 형태로** 깨졌다.

- `증가` -> `증카`, `중가`, `종카`, `증로`
- `▲` 아이콘 -> `"`, `속`, `A`
- 열 구분 통로에 낀 노이즈 때문에 옆 열 라벨과 병합
- 버튼 텍스트 `가공 하기 (979)` 가 가짜 옵션으로 파싱

신뢰도 점수로 거르는 것도 측정해보고 버렸다 — `▲`->`A` 가 87점, 오독한 `함` 이 91점,
정답 `의` 가 65점이었다. **신뢰도와 정답률에 상관이 없다.**

근본 원인은 도구 선택이다. 이 화면의 글자는 게임이 고정 폰트로 렌더한, 종류가 정해진
문자열이다. 즉 "인식" 문제가 아니라 **"정해진 후보 중 어느 것인가" 하는 조회 문제**다.
범용 OCR 은 열린 문자 공간을 가정하므로 틀릴 때도 그럴듯한 글자를 뱉고, 틀렸다는 걸
알 방법이 없다. NCC 는 점수를 주므로 **"모르겠다"고 말할 수 있다.**

같은 결론에 도달한 선례가 있다 (`airplaner.github.io/lostark-arkgrid-gem-locator-v2`).
해당 사이트의 워커를 뜯어보면 OCR 이 없고 템플릿 매칭만 있다.

## 어떻게 읽나

### 1. 앵커로 원점과 배율을 잡는다

절대 좌표를 쓰지 않는다. 창 위치가 바뀌면 전부 틀리기 때문이다.
**"다음 항목 중 무작위로 적용됩니다."** 한 줄을 찾고 나머지는 전부 거기서의 상대 위치다
(`layout.js` 의 `REF`). 이 문장은 가공 화면에 항상 있고 폭이 220px 이라 오탐이 없다.

전탐색은 80억 연산이라 불가능해서 1/4 축소본에서 대충 찾고 원본에서 주변만 다시 훑는다
(`ncc.locate`). 2048x1280 전체 화면 기준 130ms.

**위치만 맞추면 안 된다.** NCC 는 배율에 전혀 관대하지 않다 - 2.3% 만 달라도 앵커 점수가
**1.000 에서 0.56 으로** 떨어진다(실측: 2048x1280 을 2000x1250 으로 리샘플한 캡처).
그래서 `ncc.findScale` 로 배율을 먼저 찾고 화면 쪽을 기준 배율로 되돌린다. 템플릿마다
크기를 바꾸는 대신 화면을 되돌리면 이후 코드는 배율을 전혀 몰라도 된다.
배율 탐색은 2초쯤 걸리므로, 같은 화면을 반복해서 읽을 때는 호출부가 `scale` 을 넘겨 건너뛴다.

실제 다른 해상도로도 확인했다. 1920x1080 으로 게임이 직접 렌더한 화면에서 배율 **0.935**
(= 1920/2048 = 0.9375 에 근접)를 찾아내고 4개를 전부 맞춘다.
해상도가 바뀌면 **패널이 놓이는 자리 자체가 달라진다는 점**에 주의 - 2048x1280 은 16:10,
1920x1080 은 16:9 라 세로 위치가 크게 어긋난다. 그래서 앵커로 원점을 잡는 방식이 필요했다.

### 2. 옵션명은 통째로 매칭한다

마진이 넉넉하다 — 같은 문자열 **0.997~1.000**, 다른 문자열 **0.256~0.261**.

### 3. 값은 쪼개서 매칭한다

통째로 매칭하면 안 된다. 실측값:

| | Lv. 1 증가 | +2 증가 | +1 증가 | Lv. 2 증가 |
|---|---|---|---|---|
| **Lv. 1 증가** | 1.000 | 0.688 | 0.791 | **0.907** |
| **+2 증가** | 0.688 | 1.000 | **0.892** | 0.805 |

정답이 1.000 이어도 오답이 0.907 이면 마진이 0.09 뿐이다. 글자 대부분이 같고 숫자 한
글자만 다르니 당연하다. 이 상태로 쓰면 언제 뒤집혀도 이상하지 않다.

그래서 **접두(`Lv.` / `+`) · 숫자 · 접미(`증가`)** 로 나눠서 각각 맞춘다.

숫자는 글리프에 딱 맞추지 않고 **고정폭 8px 창**을 쓴다. 좌우 여백이 패턴에 들어가서
얇은 `1`(폭 2px)과 넓은 `2`(폭 6px)가 폭 자체로 갈린다. 마진이 **0.09 -> 0.60** 으로
회복된다 (같은 숫자 0.93~0.96, 다른 숫자 0.24~0.33).

창 위치를 글자 중심으로 계산해서 **고정하면 안 된다.** 글리프가 2~6px 라 1px 만
어긋나도 점수가 1.000 -> 0.575 로 떨어진다(실측). 접두와 접미 사이 구간에서 템플릿을
미끄러뜨리고 최고점을 고른다 — 정렬은 NCC 가 알아서 맞춘다.

값의 문법이 하나가 아니다. 실제 캡처로 확인된 형태:

| 화면 표기 | 구조 |
|---|---|
| `Lv. 3 증가` · `+4 증가` · `-1 감소` · `Lv. 1 감소` | 접두 + 숫자 + 접미 |
| `1회 증가` · `2회 증가` | 숫자 + 접미 (접두 없음) |
| `효과 변경` · `유지` · `+100% 증가` · `-100% 감소` | 숫자 없음, 통짜 |

어떤 문법으로 읽을지는 **옵션명이 결정한다** (`가공 상태` -> 유지, `가공 비용` -> ±100%,
`다른 항목 보기` -> N회). 옵션명은 마진이 0.7 이상이라 이 분기가 가장 믿을 만하다.

한 덩어리로 보이는 표기가 실제로는 두 줄로 갈린다는 점에 주의:
`가공 상태 유지` 는 옵션명 `가공 상태` + 값 `유지` 이고, 비용은 `가공 비용` + `+100% 증가` 다.
감소는 부호를 찍는다 - `-1 감소`, `-100% 감소`.

숫자 템플릿은 좌표가 아니라 **글자 덩어리 순번**으로 잘라낸다. 크롭이 1~2px 만 밀려도
좌표로 자르면 그대로 밀리는데, 숫자는 폭이 2~6px 라 그 정도로도 창이 빗나간다
(실측: 좌표로 자른 템플릿은 숫자끼리 최악 0.692 까지 붙었고, 덩어리로 자르니 0.355 로 벌어졌다).

### 4. 같은 문자열이라도 템플릿을 여러 개 둔다

리샘플된 캡처에서는 **같은 리샘플 캡처에서 자른 템플릿이 무조건 이긴다.** 화질이 아니라
출처가 점수를 가른다:

| 대상 | plus | minus | jeungga | gamso |
|---|---|---|---|---|
| 선명한 원본 (`+2 증가`) | **1.000** | 0.553 | **0.843** | 0.315 |
| 리샘플 캡처 (`+1 증가`) | 0.706 | **0.718** | 0.414 | **0.489** |

아래 줄이 전부 오답이다. `minus`/`gamso` 만 리샘플 캡처에서 잘랐기 때문에 생긴 편향이다.
그래서 같은 문자열이라도 여러 출처에서 잘라 두고(`manifest.json` 의 같은 `key`)
매칭은 그중 최고점을 쓴다. 이 하나로 40개 중 13개 오답이 0개가 됐다.

### 5. 애매하면 의심으로 표시한다

옵션명/값 각각 점수 0.8, 옵션명은 2등과의 마진 0.15 를 넘어야 `confident: true` 다.
하나라도 미달이면 그 열은 통째로 의심이다. 틀린 값을 조용히 넘기는 것보다 낫다.

의심 표시는 "틀렸다"가 아니라 "사람이 확인하라"는 뜻이다. 지금 40개 중 4개가 걸리는데
전부 `아군 피해 강화` 와 `아군 공격 강화` 처럼 한 글자만 다른 옵션명이다(마진 0.10).
넷 다 정답이었지만 임계값을 낮추면 경계가 헐거워지므로 그대로 뒀다.

## 검증 상태

`node web/gempago/vision/test.js` — **109개 통과**.
캡처 49장 x 4개 = **옵션 196개 전부 정답** (배율을 알려준 경우).
다이아(현재 수치)는 아래 전용 절 참고.

정답표는 `fixtures/captures.json` 에 있다. 눈으로 읽어서 적었다. 다이아 값은
**젬 포인트 = 네 수치의 합** 성질로 검산했다. 초기 캡처는 앵커 주변만 잘라
저장했고(원본 4.5MB 씩을 저장소에 넣지 않기 위해) cap27 부터는 원본 그대로다.

캡처 49장이 커버하는 것:

- 젬 계열: 혼돈(침식), 질서(견고·안정)
- 효과 이름: 공격력 · 보스 피해 · 추가 피해 · 아군 공격 강화 · 아군 피해 강화 · 낙인력 등
- 옵션 숫자 1~4 (증감폭 최대가 +4 라 5 는 옵션에 안 뜬다), 다이아 숫자 1~5
- `증가` / `감소` — 수치(`-1 감소`)와 효과(`Lv. 1 감소`) 양쪽
- `효과 변경` · `유지` · `+100% 증가` · `-100% 감소` · `1회 증가` · `2회 증가`
- 배율 3종: 1.0 (2048x1280) 4장, 0.98 (리샘플) 12장, **0.935 (1920x1080 네이티브) 33장**
- 등급 3종: 고급(5회) · 희귀(7회) · 영웅(9회)
- 가공 비용 3종: 0골드(-100%) · 900(기본) · 1,800(+100%)
- 배경판 조합 5종: 혼돈 x1 · 혼돈 x0.98 · 혼돈 x0.935 · 질서 x0.98 · 질서 x0.935
  (등급은 조합에 안 넣었다 - 고급·희귀·영웅을 섞어도 정확도가 안 떨어졌다)

**실사용 조건**(전체 아틀라스, 처음 보는 화면이 아닌 경우)에서는 347칸 전부 정답이고
조용한 오답이 0 이다. `test.js` 가 이 조건을 따로 지킨다. 아래 각 절의 정확도 수치는
전부 leave-one-out 이라 그보다 비관적이다.

### 아직 없는 것

- **효과 이름**이 게임 전체로는 더 있을 수 있다. 아틀라스에 없는 이름은 읽지 못하고
  거부한다. 지금 12종(효과 6 + 수치 3 + 특수 3)이고 실사용에서 새 이름은 안 나왔다.
- 2560x1440, 3840x2160 같은 더 큰 해상도. 지금 검증된 배율은 0.935 ~ 1.0 구간뿐이다.
  1920x1080 전체창이 사실상 표준이라 우선순위에서 뺐다.
- 표본이 한 장뿐인 값들(리롤 3회, 비용 1,800, 젬 포인트 두 자리 "2"). 실사용에서는
  맞지만 leave-one-out 에서는 못 읽는다. 같은 화면이 한 장 더 생기면 해소된다.

## 게임 밝기 설정은 신경 쓸 필요 없다

다른 도구들은 "게임 밝기를 70 이상으로 맞춰라" 같은 조건을 요구하기도 한다.
여기서는 필요 없다. NCC 가 평균을 빼고 표준편차로 나누기 때문에 **선형 밝기/대비
변화에는 원리상 완전히 무관**하고, 실제로 그렇다(`test.js` 의 "밝기/대비 불변" = 1.0000).

게임 밝기 설정은 보통 감마(비선형)라 원리만으로는 안 되고 재봐야 한다. 재봤다:

| 변형 | 옵션 60개 정답 |
|---|---|
| 원본 | 60/60 |
| 감마 0.5 / 0.7 / 1.4 / 2.0 | 60/60 |
| 대비 x0.5 | 60/60 |
| 대비 x1.8 -40 | 60/60 |
| 어둡고 밋밋하게 (x0.35 +140) | 60/60 |

**정말로 깨지는 건 포화뿐이다.** 밝기를 올려서 글자가 흰색으로 날아가면 정보 자체가
사라지므로 어떤 방법으로도 복구할 수 없다. 한계는 이렇다:

| 포화 픽셀 비율 | 옵션 60개 정답 |
|---|---|
| ~4.5% | 60/60 |
| 6.3% | 44/60 |
| 9.5% | 16/60 |

4.5% 포화를 만들려면 8비트 기준 밝기를 +130 올려야 한다. 정상적인 게임 설정으로는
근처도 못 간다. 즉 **화면이 하얗게 타 보이지만 않으면 된다.**

## JPG 로 줘도 된다

캡처를 PNG 가 아니라 JPG 로 받아도 되는지 재봤다. JPEG 는 8x8 DCT 블록 단위로 뭉개고
색차를 절반으로 줄이므로(4:2:0), 글자 가장자리에 링잉이 생긴다. 글자가 2~6px 인
숫자 창에는 위험해 보인다. 실제로는 아니었다.

15장을 품질별로 JPEG 인코딩했다가 되돌려서 그대로 읽혔다(subsampling 4:2:0).

| 품질 | 옵션 60개 정답 | 의심 표시 |
|---|---|---|
| 원본 PNG | 60/60 | 5 |
| q95 ~ q40 | 60/60 | 5 |
| q30 / q20 | 60/60 | 9 |
| q10 | 60/60 | 10 |
| q5 | 57/60 | 36 |

배율 탐색도 멀쩡하다. q40 에서 앵커 점수가 1.000 -> 0.983 (2048 네이티브),
0.937 -> 0.921 (0.98 리샘플), 0.735 -> 0.728 (1920 네이티브) 로 내려갈 뿐이고
셋 다 배율은 정확히 맞힌다.

**q10 까지 무손실과 차이가 없다.** 어떤 도구도 q10 으로 저장하지 않으므로 실무적으로는
JPG 여부를 신경 쓸 필요가 없다. q5 에서 처음 깨지는데, 틀리는 방식이 `-1 감소` ->
`효과 변경` 처럼 값 전체를 놓치는 쪽이라 의심 표시가 5 -> 36 으로 먼저 튄다.
즉 깨질 때는 조용히 틀리지 않는다.

주의할 것은 압축이 아니라 **크기 변경**이다. 캡처를 축소해서 보내면 배율과 템플릿
출처가 같이 달라지므로(위 4번 항목) 그쪽이 훨씬 위험하다.

## 다이아 (현재 수치) - 읽는다

젬의 현재 수치 4개(의지력 효율 · 포인트 · 좌/우 효과)를 다이아에서 읽는다
(`reader.readDiamonds`). 화면 공유 한 번에 옵션 4개 + 현재 수치 4개(+ 젬 계열,
효과 이름)가 채워진다. 의심 표시된 자리는 채우지 않고 알려준다.

템플릿·배경판은 손으로 뜨지 않고 `make-diamond-templates.js` 가 만든다. 캡처가 늘면
그 스크립트를 다시 돌려 `templates/` 와 manifest 를 갱신하고, leave-one-out 정확도도
같이 찍는다. **leave-one-out**(자기 캡처에서 뜬 템플릿은 빼고 맞추기, 49장 x 4자리):

- 이름 **196/196** (최저점 0.873, 최소 마진 0.032)
- 값 **188/196**, 검산까지 하면 **192/196**. 오답 전부 의심 표시 - 조용히 틀리는 값이 0 이라는 게 핵심 성질이다
- 오답 원인은 전부 표본 희소성: 그 (자리 x 배율) 조합에 그 숫자 표본이 자기 하나뿐

### 이름: 옵션 행과 같은 방식이 통한다

띠보다 좌우 8px 좁게 뜬 템플릿을 띠 안에서 미끄러뜨린다(미끄러질 자리를 안 주면
마진이 음수가 된다 - 위 3번 항목과 같은 실수를 두 번 했다). 좌/우 자리는 템플릿을
공유해도 된다. 자리마다 나올 수 있는 이름이 정해져 있어서(위 = 의지력 효율,
아래 = 혼돈/질서 포인트, 좌/우 = 효과) 후보를 자리로 거른다.

### 숫자: 배경을 빼고 마지막 덩어리만 맞춘다

값 띠는 160px 인데 글자는 3~12px 라 통짜 비교는 배경이 점수를 다 먹는다(마진 0.005).
띠를 z 정규화한 뒤(밝기를 먼저 없애야 배경판이 밝은 캡처에 끌려가지 않는다)
배경판을 빼고, 잔차의 **마지막 글자 덩어리**(= 숫자. 좌/우는 `Lv. N` 이라 `Lv.` 잔재가
앞에 남는다)를 자리별 숫자 템플릿과 맞춘다.

대각선 장식은 캡처마다 1px 씩 어긋나서 잔차에 글자만큼 밝은 유령 줄무늬를 남긴다.
글자는 배경보다 밝아지기만 하고(잔차가 양수뿐) 유령은 같은 열에 +/- 쌍으로 오므로,
열 점수를 "양수 최대 - 음수 최대" 로 잡으면 유령만 지워진다 (`reader.residualSpans`).

### 앞서 적었던 결론 두 개가 28장 재측정으로 또 틀렸다

**"픽셀별 중앙값 배경판" -> 틀렸다.** 15장에서는 값이 흩어져 있어 통했지만, 28장에서는
아래 다이아의 절반이 숫자 "1" 이라 그 획이 중앙값에 박힌다. 그러면 값이 1 인 캡처는
잔차가 사라지고 다른 캡처에는 유령 획이 남는다. 지금은:

- 배경판을 **(젬 종류 x 배율) 조합마다** 따로 만든다. 다이아 아트가 이 두 축에 따라
  픽셀 수준에서 달라서, 한 판으로 뭉치면 잔차 노이즈가 0.3+ 로 올라가고
  같은 조합끼리는 0.02~0.11 로 깨끗하다(실측).
- 실행 시에는 판을 전부 빼보고 **잔차가 가장 조용한 판**을 고른다. 맞는 판이면 글자만
  남고 틀린 판이면 무늬가 남으므로 자기 선택이 된다. 조용한 판에서 글자가 안 나오면
  다음 판으로 넘어간다(조합 전체가 같은 숫자면 그 숫자가 배경판에 흡수되는 경우의 폴백).
- 한 판 안에서는 하위 20% 분위수로 초벌을 잡고 글자로 판정된 표본을 빼고 평균을
  다시 낸다. 중앙값보다 반복 글자에 강하다.

**"숫자 템플릿은 네 자리가 공유한다" -> 틀렸다.** 네 자리의 숫자 폰트 크기가 전부
조금씩 다르다 (실측: 위 "1" 7x12, 아래 "1" 6x11, 좌 "1" 12x16 - 교차 점수가 0.4~0.6
대로 떨어져 같은 숫자끼리도 진다). 자리마다 따로 모은다. 다행히 캡처 39장으로 숫자
1~5 가 네 자리 대부분에서 이미 모였다.

### 남은 한계 - 전부 의심 표시로 떨어진다

- **조합 전체가 같은 숫자**면(x1 조합 4장은 위/오른쪽이 전부 1) 그 숫자가 배경판에
  흡수된다. 폴백 판으로 읽지만 점수가 낮아 의심으로 표시된다.
- **표본이 하나뿐인 (자리 x 배율 x 숫자) 조합**은 leave-one-out 에서 틀린다.
  같은 조건의 캡처가 한 장만 더 생기면 해결된다. 어떤 젬이든 어떤 수치든 상관없고
  골드가 들지 않는다.

### 젬 포인트 합으로 검산한다

`젬 포인트` 숫자는 **네 수치의 합과 정확히 일치한다** (실측: 캡처 전부). 그래서
"젬 포인트 N" 을 따로 읽고(`reader.readGemPoint` - 줄이 가운데 정렬이라 이름을
템플릿 앵커로 찾는다. 두 자리 10~20 지원) 검산한다 (`reader.reconcileGem`):

- 넷 중 **하나만 애매하면 그 자리는 계산으로 복구**된다 - 템플릿이 없어 못 읽던
  숫자도 답이 나온다. leave-one-out 에서 11건 복구, 틀리게 복구 0건.
  검산까지 하면 값 정답이 147 -> **150/156** (남은 6개는 젬 포인트 줄이 잘려
  나간 초기 크롭 캡처이거나 둘 이상 애매한 경우로, 전부 의심 표시).
- 넷 다 확실한데 **합이 안 맞으면 넷 다 의심으로 내린다** - 어디가 틀렸는지 알 수
  없기 때문이다. 자동 입력은 멈추고 사람에게 알린다.

숫자 창 위치를 이름 끝(x + 템플릿 폭)에서 파생시키면 안 된다 - 템플릿 변형마다
잘린 폭이 1~3px 달라서 창이 그만큼 밀린다. 이름 "시작 + 고정 간격" 으로 잡는다.

## 리롤/가공 횟수/가공 비용도 읽는다

리롤 버튼("N회 가능"), 가공하기 버튼("가공 하기 (N/M)"), 가공 비용 금액을 읽는다
(`reader.readMeta`). N 이 남은 가공, M 이 등급(5/7/9)이다. 배경이 민무늬 버튼이라
옵션 행과 같은 방식(고정 창 + 미끄러뜨리기)이면 되고, 배경판이 필요 없다.
템플릿·측정은 `make-meta-templates.js`. **leave-one-out 182/185**, 오답 3개는 전부
표본이 자기 하나뿐인 경우(리롤 3회, 젬 포인트의 두 자리 "2", 비용 1,800)이고 전부
의심 표시된다.

**가공 비용은 금액을 통짜로 분류한다.** 화면은 배율이 아니라 금액을 보여주므로
`0 / 900 / 1,800` 을 읽고 `-100% / 기본 / +100%` 로 뒤집는다. 자릿수별로 쪼개면
`900` 의 끝 `0` 이 `0` 으로 읽히는 함정이 생기는데, 오른쪽 정렬이라 창 안 위치가
고정이고 후보가 닫힌 집합이라 통짜가 더 안전하다 (33/34, 점수 0.998).
`+100%(1,800)` 는 표본이 한 장뿐이라 leave-one-out 에서는 못 읽지만 실사용에서는
맞는다. 모르는 금액은 null 을 돌려 그 칸을 건드리지 않는다. 기준 금액 900 은
고급·희귀·영웅 세 등급 모두 같았다.

숫자 창 폭은 **좁게** 잡아야 한다. 처음엔 이웃 글자(`(`, `/`)를 일부러 넣었는데,
그 글자는 모든 후보가 공유하므로 상관에서 차이를 묻어버린다 - `(7/7)` 의 정답
마진이 **0.032** 라 가장 흔한 화면이 늘 의심으로 빠졌다. 창을 숫자 폭(11px)으로
줄이니 마진이 **0.29** 로 올라가고 의심 표시가 12 -> 4 로 줄었다.

대신 좁은 창은 덩어리 기준으로 자를 수 없다 - `6` 은 `/` 와 붙어 한 덩어리가 되고
`5` 는 갈라져서 잘림 위치가 숫자마다 달라진다. N·M 이 항상 한 자리라 위치가
고정이므로 **띠 기준 고정 좌표**로 자른다 (`ATTEMPT_INSET`).

세로는 글자에 맞게 타이트하게 자른다. 띠 높이 그대로 자르면 세로로 미끄러질 틈이
없어서 버튼 위치가 1px 만 어긋나도 무너진다.

이로써 화면 공유 한 번에 계산기 입력이 **목표 빼고 전부** 채워진다: 옵션 4 +
현재 수치 4 + 젬 계열 + 효과 이름 2 + 등급 + 남은 가공 + 리롤 + 가공 비용.

## 파일

| 파일 | 용도 |
|---|---|
| `ncc.js` | NCC 매칭, 축소 탐색, 배율 탐색. 브라우저/Node 공용, 의존성 없음 |
| `layout.js` | 앵커 기준 기하 구조 + 배율 정규화. 글자 덩어리 분할 |
| `reader.js` | 4개 항목 읽기. 옵션명 계열에 따라 값 문법을 갈라 읽는다 |
| `interpret.js` | 읽은 글자 <-> `rules.js` 항목 id. 이미지를 안 만져서 캡처 없이 검증된다 |
| `atlas.js` | 템플릿 로더 (Node 전용) |
| `png.js` | PNG 디코드/인코드. **Node 전용**(zlib) |
| `make-diamond-templates.js` | 다이아 템플릿·배경판 생성 + leave-one-out 측정. 캡처가 늘면 다시 돌린다 |
| `make-meta-templates.js` | 리롤·가공 횟수·젬 포인트·가공 비용 템플릿 생성 + 측정 |
| `templates/` | 아틀라스 + `manifest.json` (문자열·출처·계열·띄어쓰기 규칙) |
| `fixtures/` | 캡처 49장 + 정답표 `captures.json` |
| `scratch-assets/` | 탐색 중 잘라둔 것들. **출처와 배율이 서로 안 맞는다** |

템플릿 파일명이 ASCII 인 이유는 한글 파일명의 NFC/NFD 정규화가 플랫폼마다 달라서
체크아웃 후 경로가 안 맞는 일이 생기기 때문이다. 실제 문자열은 `manifest.json` 에 있다.

`scratch-assets/` 는 초기 검증 세션의 잔재다. 저장된 PNG 끼리 배율이 맞지 않아
(실측: 서로 0.14~0.18) 여기서 회귀 테스트를 만들 수 없다. 참고용으로만 남겼다.
