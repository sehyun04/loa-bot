/**
 * 머메이드 다이어그램 문자열.
 *
 * 스크린샷이 없어도 구조는 그림으로 설명할 수 있다. 여기 담은 것은 자체 호스팅
 * 파인튜닝 모델과 상용 API 가 같은 요청을 어떻게 다르게 처리하는지다.
 *
 * 세 가지가 갈린다 - 도구를 누가 고르는가, 말투를 어디서 만드는가,
 * 비용이 무엇에 붙는가. 교체 판단도 이 세 축에서 나왔다.
 *
 * 렌더는 MermaidDiagram 컴포넌트에 문자열로 넘긴다.
 */

/**
 * 자체 호스팅 파인튜닝 모델 - 분류기를 사람이 짠다.
 *
 * 작은 모델은 "도구를 골라 부르는" 판단을 믿고 맡길 수 없다. 그래서 입력을
 * 먼저 갈라주는 그래프를 손으로 짜야 한다. 규칙이 잡아내지 못하는 표현은
 * 그냥 일상 대화로 흘러가고, 새 도구를 붙일 때마다 분기와 키워드를 함께 늘려야 한다.
 */
export const LOCAL_LANGGRAPH = `flowchart LR
  U["카드알림 설정해줘"] --> C["분류 노드<br/>키워드 규칙을 사람이 작성"]
  C -->|"규칙에 걸림"| T["도구 실행 분기<br/>배선도 사람이"]
  C -->|"규칙에 안 걸림"| G["일상 대화로 처리"]
  T --> M["파인튜닝 모델<br/>말투는 학습으로 주입"]
  G --> M
  M --> R["응답"]

  N1["표현이 바뀌면 못 잡는다<br/>도구가 늘면 분기도 늘어난다"] -.-> C
`

/**
 * 상용 API - 모델이 스스로 고른다.
 *
 * 도구 목록만 넘기면 어느 것을 부를지, 인자를 어떻게 채울지 모델이 판단한다.
 * 분류기가 사라지므로 유지할 규칙도 사라진다. 말투는 학습이 아니라
 * 지시문과 예시 대화로 잡는다 - 바꾸려면 문장만 고치면 된다.
 */
export const API_TOOL_CALLING = `flowchart LR
  U["카드알림 설정해줘"] --> A["도구 목록을 함께 전달"]
  A --> L["모델이 스스로 판단<br/>어느 도구를 어떤 인자로"]
  L --> T["도구 실행"]
  T --> R["응답"]

  subgraph TONE["말투를 정하는 곳"]
    S1["성격 지시문"]
    S2["예시 대화"]
    S3["금지 규칙"]
  end
  TONE --> L

  N1["분류기가 필요 없다<br/>표현이 달라도 알아서 고른다"] -.-> L
`

/**
 * 백엔드 교체 지점.
 *
 * 환경변수 한 줄로 양쪽을 바꿔 끼울 수 있게 짜 두면 같은 질문을 양쪽에 던져
 * 비교할 수 있다. 이 구조가 있어서 교체를 감이 아니라 측정으로 정했다.
 */
export const BACKEND_SWITCH = `flowchart TB
  Q["같은 질문 묶음"] --> SW{"백엔드 전환<br/>환경변수 한 줄"}
  SW -->|"자체 호스팅"| A["파인튜닝 모델 · GPU"]
  SW -->|"상용 API"| B["상용 API"]
  A --> AM["긴 대화에서 느린 5%가 24.7초<br/>GPU 시간만큼 비용"]
  B --> BM["긴 대화에서 느린 5%가 2.3초<br/>쓴 토큰만큼 비용"]
  AM --> J["같은 기준으로 채점"]
  BM --> J
  J --> D["품질은 대등<br/>지연과 비용에서 갈렸다"]
`

/**
 * 세 축 비교 - 무엇이 실제로 달라졌는가.
 */
export const TRADEOFF_TABLE = `flowchart LR
  subgraph SELF["자체 호스팅 파인튜닝"]
    direction TB
    SA["도구 선택<br/>사람이 분류기 작성"]
    SB["말투<br/>데이터 만들어 학습"]
    SC["비용<br/>GPU 시간에 붙는다"]
  end

  subgraph API["상용 API"]
    direction TB
    AA["도구 선택<br/>모델이 스스로"]
    AB["말투<br/>지시문과 예시로"]
    AC["비용<br/>쓴 토큰에 붙는다"]
  end

  SELF --> API
`

/**
 * 교체를 결정하기까지 거친 순서.
 *
 * 실패한 시도를 지우지 않는 게 중요하다 - 무엇을 왜 접었는지가 판단의 근거고,
 * 성공만 남기면 운이 좋았던 것과 구별되지 않는다.
 */
export const MODEL_JOURNEY = `flowchart LR
  S1["파인튜닝 시도<br/>과적합"] --> S2["다른 모델<br/>환경 미지원"]
  S2 --> S3["파인튜닝 성공<br/>자체 호스팅 운영"]
  S3 --> S4["운영에서 드러난 문제<br/>지연 · GPU 비용 · 긴 대화 취약"]
  S4 --> S5["상용 API 로 교체<br/>측정 후 채택"]
`
