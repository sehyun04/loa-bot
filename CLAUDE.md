# loa-bot

@AGENTS.md

<!--
규칙 본문은 AGENTS.md 하나에 둔다. 같은 저장소에 규칙 문서가 둘이면 한쪽만
고쳐지고, 그때부터 어느 쪽이 맞는지 알 수 없게 된다. 실제로 이 파일과
AGENTS.md 에 이모지 규칙이 두 벌로 적혀 있었다.

원본을 AGENTS.md 로 둔 건 Codex 쪽 사정이다 - Codex 는 AGENTS.md 를 자동으로
읽지만 import 문법이 없어서, 여기에 본문을 두면 Codex 는 "가서 읽어라" 를
한 단계 더 거쳐야 하고 그 단계는 건너뛰기 쉽다. 반대로 Claude Code 는 위의
@ import 로 전문을 그대로 받으므로 손해가 없다.
-->
