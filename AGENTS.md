# loa-bot

디스코드 봇(`run/`, `main.py`)과 정적 웹 계산기(`web/gempago/`)가 한 저장소에 있다.
둘은 서로 의존하지 않는다.

## 스타일

- **이모지 금지.** 새로 작성/수정하는 코드에는 이모지를 쓰지 않는다. 봇이 보내는 메시지도 포함.
  아이콘이 필요하면 유니코드 이모지(📍🃏 등) 대신 텍스트나 이미 쓰던 기호(`·`, `└`, `-#` 등)를 쓴다.
  웹처럼 SVG 를 쓸 수 있는 곳에서는 SVG 로 그린다.
- **주석은 why 만.** 코드가 이미 말하는 what 은 반복하지 않고, 그렇게 짠 이유·배경·제약만 남긴다.
- 문서와 주석은 한국어.

## 작업 전에 읽을 것

젬파고(젬 가공 계산기)를 건드린다면 순서대로:

1. [`web/gempago/README.md`](web/gempago/README.md) - 계산 방식, 구조, UI 설계 이유, 남은 일
2. [`web/gempago/vision/README.md`](web/gempago/vision/README.md) - 화면 인식 전부.
   **이미 측정해서 버린 방법들이 적혀 있다.** 여기 "틀렸다"고 적힌 걸 다시 시도하지 말 것

## 검증

바꾼 뒤에는 반드시 둘 다 통과해야 한다.

```
node web/gempago/test.js          # 확률 표 + 솔버 (26)
node web/gempago/vision/test.js   # 화면 인식 (109) - 캡처 49장을 다 읽어서 1분쯤 걸린다
```

웹은 빌드 단계가 없다. 로컬 서버로 띄워서 눈으로 본다.

```
cd web/gempago && python -m http.server 8080
```

`fetch` 와 Web Worker 를 쓰므로 `file://` 로 열면 안 된다.

## 젬파고에서 어기면 안 되는 것

### 확률 표를 임의로 고치지 않는다

`rules.js` 는 스마일게이트 공식 확률 공개 페이지 값이고 27개 합이 정확히 100.0000% 다.
추정치가 아니다. 숫자 하나를 바꾸면 결과 전체가 거짓이 된다. 근거 없이 손대지 말 것.

### 조용히 틀리지 않는다

이 프로젝트의 핵심 불변식이다. 화면 인식은 **모르면 모른다고 해야 한다.**

- 확신이 없는 값은 입력칸에 넣지 않고 의심 표시로 남긴다. 넣고 나서 사용자가
  알아서 검증하기를 기대하지 않는다.
- 젬 포인트(= 네 수치의 합)로 검산한다. 한 자리가 애매하면 합으로 복구하고,
  합이 안 맞으면 네 자리 전부 의심으로 내린다(`reader.reconcileGem`).
- 접이식 패널은 접혀 있어도 **무엇이 들어갔는지 한 줄 요약을 보여준다.**
  안 보이면 잘못 읽은 값으로 계산된 답을 그대로 믿게 된다.
- 화면에는 보이는데 지금 수치에서 나올 수 없는 항목이면 알린다.

점수 문턱을 낮춰서 "인식률"을 올리는 변경은 이 불변식을 깨는 것이다. 하지 말 것.

### 문구의 주인은 index.html 이다

사용자에게 보이는 문구는 `index.html` 에 있고 `app.js` 가 그걸 읽어다 쓴다
(`IDLE_LIVE = $('liveState').textContent`). 같은 문장을 두 파일에 두면 한쪽만 고쳐진다.

- **`app.js` 에 안내 문구를 하드코딩하지 말 것.**
- 저장소 주인이 `index.html` 을 직접 고친다. 문구를 "고쳐 주지" 말 것.
- HTML 에서 요소가 빠져도 `app.js` 가 터지면 안 된다. 실제로 `pickSummary` span 이
  빠졌을 때 `null.textContent` 로 계산 전체가 죽은 적이 있다. 널 가드를 둔다.

### app.js 는 확률을 계산하지 않는다

전부 워커 안의 `solver.js` 가 한다. UI 쪽에서 확률을 흉내내면 `test.js` 가
검증하지 못하는 두 번째 진실이 생긴다.

## 워크트리

같은 저장소가 두 곳에 체크아웃돼 있다.

| 경로 | 브랜치 |
|---|---|
| `C:\Users\kshkj\Desktop\sehyun\loa-bot` | `main` |
| `C:\Users\kshkj\Desktop\sehyun\loa-bot-gempago` | `gempago-calculator` |

`main` 이 다른 워크트리에 체크아웃돼 있어서 `git checkout main` 이 안 된다.
작업은 브랜치에서 하고, 반영은 `main` 워크트리에서 `git merge` 한다.

원격 푸시는 저장소 주인이 검증한 뒤에 한다. **먼저 푸시하지 말 것.**
